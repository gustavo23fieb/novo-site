<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - FIEBTECH</title>

    <!-- FAVICON -->
    <link rel="icon" type="image/png" href="https://stgwebfiebbrs001.blob.core.windows.net/web/favicon.png" />

    <!-- BOOTSTRAP -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />

    <!-- FONT AWESOME -->
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.5.0/css/all.css" />

    <!-- CSS GLOBAL DO PROJETO -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/site.styles.css" asp-append-version="true" />

    <!-- ESTILOS GERAIS DO LAYOUT FIEBTECH -->
    <style>
        :root {
            --ft-purple: #6b1c86; /* faixa do topo */
            --ft-purple-dark: #501567;
            --ft-blue: #0075c9;
            --ft-text-light: #ffffff;
            --ft-menu-text: #ffffff;
            --ft-orange: #ff7b22;
            --max-width: 1180px;
            --header-height: 76px;
            --menu-mobile-max: 520px;
        }

        * {
            box-sizing: border-box;
        }

        html, body {
            margin: 0;
            padding: 0;
            width: 100%;
            min-height: 100%;
            font-family: Arial, Helvetica, sans-serif;
            overflow-x: hidden;
            background: #ffffff;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        img {
            max-width: 100%;
            display: block;
        }

        /* ==========================
                   HEADER FIEBTECH
                =========================== */
        .ft-header {
            position: relative;
            z-index: 50;
            background: linear-gradient(to right, var(--ft-purple), var(--ft-purple-dark));
            color: var(--ft-text-light);
            box-shadow: 0 2px 4px rgba(0, 0, 0, .25);
        }

        .ft-header-inner {
            max-width: var(--max-width);
            margin: 0 auto;
            padding: 10px 18px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 32px;
            height: var(--header-height);
        }

        /* LOGO FIEBTECH (texto estilizado) */
        .ft-logo {
            display: flex;
            flex-direction: column;
            gap: 2px;
        }

        .ft-logo-main {
            font-size: 24px;
            font-weight: 800;
            letter-spacing: .08em;
            text-transform: uppercase;
            line-height: 1;
        }

            .ft-logo-main span:first-child {
                color: #ffffff;
            }

            .ft-logo-main span:last-child {
                color: transparent;
                -webkit-text-stroke: 1px #ffffff;
                text-stroke: 1px #ffffff;
            }

        .ft-logo-sub {
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: .15em;
            opacity: .9;
        }

        /* MENU DESKTOP */
        .ft-nav {
            display: flex;
            align-items: center;
            gap: 28px;
            list-style: none;
            padding: 0;
            margin: 0;
            font-size: 14px;
            font-weight: 600;
        }

            .ft-nav > li {
                position: relative;
            }

                .ft-nav > li > a,
                .ft-nav > li > button {
                    background: none;
                    border: none;
                    color: var(--ft-menu-text);
                    cursor: pointer;
                    display: inline-flex;
                    align-items: center;
                    gap: 6px;
                    padding: 4px 0;
                    white-space: nowrap;
                }

                    .ft-nav > li > a:hover,
                    .ft-nav > li > button:hover {
                        opacity: .8;
                    }

                    .ft-nav > li > button i {
                        font-size: 10px;
                    }

        /* Submenu desktop */
        .ft-submenu {
            position: absolute;
            top: 130%;
            left: 0;
            min-width: 190px;
            background: #ffffff;
            color: #333;
            border-radius: 4px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, .18);
            padding: 6px 0;
            opacity: 0;
            visibility: hidden;
            transform: translateY(6px);
            transition: all .15s ease-out;
            list-style: none;
            z-index: 60;
        }

            .ft-submenu li a {
                display: block;
                padding: 8px 14px;
                font-size: 13px;
                color: #444;
            }

                .ft-submenu li a:hover {
                    background: #f3f3f3;
                    color: var(--ft-blue);
                }

        .ft-nav > li:hover > .ft-submenu {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }

        /* BOTÃO PROCESSO SELETIVO (se precisar depois) */
        .ft-nav-highlight {
            padding: 8px 14px;
            border-radius: 999px;
            background: var(--ft-orange);
            color: #fff !important;
            text-transform: uppercase;
            font-size: 13px;
        }

        /* BOTÃO MOBILE */
        .ft-menu-toggle {
            display: none;
            background: none;
            border: none;
            color: #ffffff;
            font-size: 22px;
            cursor: pointer;
        }

        /* ==========================
                   LAYOUT DO CORPO
                =========================== */
        .ft-main {
            width: 100%;
        }

        footer {
            margin-top: 40px;
        }

        /* ==========================
                   RESPONSIVO
                =========================== */
         (max-width: 992px) {
            .ft-header-inner {
                gap: 16px;
            }
        }

         (max-width: 768px) {
            .ft-menu-toggle {
                display: block;
            }

            .ft-nav {
                position: absolute;
                top: var(--header-height);
                right: 0;
                left: 0;
                flex-direction: column;
                align-items: flex-start;
                background: linear-gradient(to right, var(--ft-purple), var(--ft-purple-dark));
                padding: 8px 0;
                max-height: 0;
                overflow: hidden;
                transition: max-height .2s ease-out;
                box-shadow: 0 10px 20px rgba(0, 0, 0, .25);
            }

                .ft-nav.open {
                    max-height: var(--menu-mobile-max);
                }

                .ft-nav > li {
                    width: 100%;
                }

                    .ft-nav > li > a,
                    .ft-nav > li > button {
                        width: 100%;
                        padding: 10px 18px;
                        justify-content: space-between;
                    }

            .ft-submenu {
                position: static;
                box-shadow: none;
                border-radius: 0;
                background: rgba(0, 0, 0, .15);
                color: #fff;
                margin-top: 0;
                max-height: 0;
                overflow: hidden;
                opacity: 1;
                visibility: visible;
                transform: none;
                transition: max-height .2s ease-out;
            }

                .ft-submenu.open {
                    max-height: 260px;
                }

                .ft-submenu li a {
                    padding: 8px 32px;
                    color: #fff;
                }

                    .ft-submenu li a:hover {
                        background: rgba(0, 0, 0, .18);
                        color: #fff;
                    }
        }
    </style>

    @RenderSection("Styles", required: false)
</head>
<body>

    <!-- HEADER FIEBTECH -->
    <header class="ft-header">
        <div class="ft-header-inner">
            <!-- LOGO -->
            <div class="ft-logo">
                <div class="ft-logo-main">
                    <span>FIEB</span><span>TECH</span>
                </div>
                <div class="ft-logo-sub">
                    Educação conectada ao futuro
                </div>
            </div>

            <!-- BOTÃO MOBILE -->
            <button class="ft-menu-toggle" id="ftMenuToggle" aria-label="Abrir menu">
                <i class="fas fa-bars"></i>
            </button>

            <!-- MENU -->
            <ul class="ft-nav" id="ftMainNav">
                <li class="ft-has-submenu">
                    <button type="button">
                        A FIEB <i class="fas fa-chevron-down"></i>
                    </button>
                    <ul class="ft-submenu">
                        <li><a href="#">História</a></li>
                        <li><a href="#">Institucional</a></li>
                    </ul>
                </li>

                <li><a href="#">Cursos</a></li>

                <li class="ft-has-submenu">
                    <button type="button">
                        Unidades <i class="fas fa-chevron-down"></i>
                    </button>
                    <ul class="ft-submenu">
                        <li><a href="#">FIEB - Aldeia da Serra</a></li>
                        <li><a href="#">FIEB - Alphaville</a></li>
                        <li><a href="#">FIEB - Engenho Novo</a></li>
                        <li><a href="#">FIEB - Jardim Belval</a></li>
                        <li><a href="#">FIEB - Jardim Paulista</a></li>
                        <li><a href="#">FIEB - Maria Cristina</a></li>
                        <li><a href="#">FIEB - Parque Imperial</a></li>
                    </ul>
                </li>

                <li><a href="#">Notícias</a></li>
                <li><a href="#">Contato</a></li>
            </ul>
        </div>
    </header>

    <!-- CONTEÚDO -->
    <div class="ft-main">
        @RenderBody()
    </div>

    <!-- FOOTER (pode customizar depois) -->
    <footer class="text-center py-3">
    </footer>

    <!-- SCRIPTS GLOBAIS -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    <script>
        (function () {
            const toggle = document.getElementById("ftMenuToggle");
            const nav = document.getElementById("ftMainNav");
            const submenuBtns = document.querySelectorAll(".ft-has-submenu > button");

            if (toggle && nav) {
                toggle.addEventListener("click", function () {
                    nav.classList.toggle("open");
                });
            }

            submenuBtns.forEach(btn => {
                btn.addEventListener("click", function (e) {
                    if (window.innerWidth <= 768) {
                        e.preventDefault();
                        const submenu = btn.parentElement.querySelector(".ft-submenu");
                        submenu.classList.toggle("open");
                    }
                });
            });

            // ao voltar pro desktop, garante reset
            window.addEventListener("resize", function () {
                if (window.innerWidth > 768 && nav) {
                    nav.classList.remove("open");
                    document.querySelectorAll(".ft-submenu").forEach(sm => sm.classList.remove("open"));
                }
            });
        })();
    </script>

    @RenderSection("Scripts", required: false)
</body>
</html>
