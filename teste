<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>FIEBTECH</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="/lib/bootstrap/dist/css/bootstrap.min.css" />

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

    <!-- Google Fonts (Poppins) -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

    <style>
        /* -----------------------
               CONFIGURAÇÕES RÁPIDAS
               ----------------------- */
        :root {
            --site-gutter: 48px; /* espaço lateral (altera o "encaixe" do header) */
            --menu-gap: 40px; /* espaçamento entre itens do menu */
            --topbar-gap: 28px; /* espaçamento entre itens da topbar */
            --header-height: 92px; /* altura visual do header (ajuste fino) */
        }

        /* =====================
               RESET BÁSICO
            ===================== */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        html, body {
            width: 100%;
            font-family: Arial, Helvetica, sans-serif;
            overflow-x: hidden;
        }

        /* =====================
               CONTAINER CENTRAL (alinha tudo igual)
            ===================== */
        .site-container {
            max-width: 1300px;
            margin: 0 auto;
            padding: 0 var(--site-gutter);
            width: 100%;
        }

        /* =====================
               TOPBAR
            ===================== */
        .topbar {
            width: 100%;
            background: #ffffff;
            border-bottom: 1px solid #e5e5e5;
            /* sem padding lateral aqui — o container controla */
        }

            .topbar .site-container {
                display: flex;
                align-items: center;
                justify-content: flex-end; /* mantém no lado direito do container */
                height: 36px;
            }

            .topbar ul {
                display: flex;
                gap: var(--topbar-gap);
                list-style: none;
                align-items: center;
            }

            .topbar a {
                color: #333;
                text-decoration: none;
                font-weight: 600;
                font-size: 13px;
                display: inline-flex;
                align-items: center;
                gap: 8px;
            }

            .topbar i {
                font-size: 13px;
            }

        /* =====================
               HEADER PRINCIPAL
            ===================== */
        .custom-header {
            width: 100%;
            background: linear-gradient(90deg, #7d2ae8, #0d6efd);
            /* altura visual controlada pelo conteúdo + header-height */
            box-shadow: inset 0 -1px rgba(0,0,0,0.06);
        }

            .custom-header .site-container {
                display: flex;
                align-items: center;
                justify-content: space-between;
                height: var(--header-height);
            }

        /* =====================
               LOGO
            ===================== */
        .logo {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: flex-start; /* logo sempre na esquerda do container */
        }

            .logo img {
                width: 170px; /* ajuste para ficar proporcional — trocar se precisar */
                height: auto;
                display: block;
            }

        .slogan {
            font-family: 'Poppins', sans-serif;
            font-size: 13px;
            color: rgba(255,255,255,0.95);
            margin-top: 6px;
            line-height: 1;
        }

        /* =====================
               MENU (centralizado no espaço entre logo e topbar)
            ===================== */
        .header-inner {
            display: flex;
            align-items: center;
            gap: 24px;
            width: 100%;
        }

        /* o nav ocupa espaço flexível entre logo e o "lado direito" do header */
        nav.main-nav {
            display: flex;
            flex: 1; /* ocupa o espaço disponível */
            justify-content: center; /* centraliza o menu no espaço */
        }

            nav.main-nav ul {
                display: flex;
                gap: var(--menu-gap);
                list-style: none;
                align-items: center;
                margin: 0;
                padding: 0;
            }

            nav.main-nav a {
                color: #fff;
                font-family: 'Poppins', sans-serif;
                font-weight: 600;
                font-size: 15px;
                text-decoration: none;
                display: inline-block;
                padding: 6px 0;
            }

                nav.main-nav a:hover {
                    opacity: 0.85;
                }

        /* =====================
               FOOTER / OUTROS
            ===================== */
        .conteudo-pagina {
            width: 100%;
            max-width: 1300px;
            margin: 40px auto;
            padding: 0 var(--site-gutter);
        }

        /* =====================
               RESPONSIVO
            ===================== */
         (max-width: 992px) {
            :root {
                --site-gutter: 28px;
                --menu-gap: 26px;
            }

            .logo img {
                width: 150px;
            }
        }

         (max-width: 768px) {
            :root {
                --site-gutter: 18px;
                --menu-gap: 18px;
                --topbar-gap: 14px;
            }

            .custom-header .site-container {
                flex-direction: column;
                height: auto;
                gap: 10px;
                padding-top: 12px;
                padding-bottom: 12px;
            }

            nav.main-nav {
                order: 2;
                width: 100%;
                justify-content: center;
            }

                nav.main-nav ul {
                    flex-wrap: wrap;
                    justify-content: center;
                    gap: 12px;
                }

            .logo {
                order: 1;
                align-items: center;
            }

            .topbar .site-container {
                justify-content: center;
            }
        }
    </style>
</head>
<body>

    <!-- Topbar -->
    <div class="topbar" role="banner">
        <div class="site-container">
            <ul>
                <li><a href="https://fieb.edu.br/soualuno" target="_blank" rel="noopener">Sou Aluno <i class="fas fa-book-reader"></i></a></li>
                <li><a href="https://fieb.edu.br/soucolaborador" target="_blank" rel="noopener">Sou Colaborador <i class="fas fa-users"></i></a></li>
                <li><a href="https://fieb.edu.br/souresponsavel" target="_blank" rel="noopener">Sou Responsável <i class="fas fa-user-shield"></i></a></li>
            </ul>
        </div>
    </div>

    <!-- Header -->
    <header class="custom-header" role="banner">
        <div class="site-container header-inner">
            <!-- Logo -->
            <div class="logo" aria-label="FIEBTECH">
                <img src="https://stgwebfiebbrs001.blob.core.windows.net/web/logo-fiebtech-branco.svg" alt="FIEBTECH">
                <div class="slogan"></div>
            </div>

            <!-- Menu central -->
            <nav class="main-nav" role="navigation" aria-label="Menu principal">
                <ul>
                    <li><a href="#">A FIEB</a></li>
                    <li><a href="#">Cursos</a></li>
                    <li><a href="#">Unidades</a></li>
                    <li><a href="#">Notícias</a></li>
                    <li><a href="#">Contato</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <!-- Conteúdo -->
    <main class="conteudo-pagina" role="main">
        <!-- sua página -->
    </main>

    <script src="/lib/jquery/dist/jquery.min.js"></script>
    <script src="/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
