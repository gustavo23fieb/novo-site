<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>FIEB - Header corrigido</title>

    <!-- Bootstrap CSS (opcional, mantive por compatibilidade) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" />

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

    <style>
        :root {
            --site-gutter: 48px;
            --menu-gap: 40px;
            --topbar-gap: 28px;
            --header-height: 500px;
        }

        /* reset mínimo */
        * {
            box-sizing: border-box
        }

        body {
            margin: 0;
            font-family: Arial,Helvetica,sans-serif;
            background: #fff;
            color: #222
        }

        /* TOPBAR */
        .topbar {
            background: #fff;
            border-bottom: 1px solid #e5e5e5;
            padding: 8px 0;
        }

            .topbar .container {
                display: flex;
                justify-content: flex-end;
                gap: var(--topbar-gap)
            }

            .topbar a {
                color: #333;
                text-decoration: none;
                font-weight: 600;
                font-size: 13px;
                display: inline-flex;
                align-items: center;
                gap: 8px
            }

        /* BANNER / HEADER */
        .header-banner {
            position: relative;
            background-image: url('https://fieb.edu.br/wp-content/themes/fieb-2022/img/home.jpg');
            background-size: cover;
            background-position: center;
            height: var(--header-height);
            display: flex;
            align-items: flex-start;
            padding: 18px var(--site-gutter);
        }

            /* overlay para contraste */
            .header-banner::after {
                content: "";
                position: absolute;
                inset: 0;
                background: linear-gradient(90deg, rgba(124,45,255,0.55) 0%, rgba(0,123,255,0.45) 100%);
                pointer-events: none;
                z-index: 1;
            }

        /* header inner (logo + nav) */
        .header-inner {
            position: relative;
            z-index: 2;
            width: 100%;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        /* logo */
        .logo img {
            width: 170px;
            height: auto;
            display: block
        }

        /* nav */
        nav.main-nav {
            display: flex;
            align-items: center;
            gap: 20px
        }

            nav.main-nav ul {
                list-style: none;
                display: flex;
                gap: var(--menu-gap);
                margin: 0;
                padding: 0;
                align-items: center
            }

            nav.main-nav li {
                position: relative
            }

            nav.main-nav a {
                color: #fff;
                text-decoration: none;
                font-weight: 600;
                font-family: Arial,Helvetica,sans-serif;
                font-size: 15px;
                display: inline-flex;
                align-items: center;
                gap: 6px;
                padding: 6px 4px;
            }

                nav.main-nav a:hover {
                    opacity: .9
                }

            /* submenu baseline */
            nav.main-nav .submenu {
                display: none;
                position: absolute;
                top: calc(100% + 6px);
                left: 0;
                background: #0d6efd;
                border-radius: 6px;
                min-width: 200px;
                z-index: 50;
                overflow: hidden;
            }

                nav.main-nav .submenu li a {
                    display: block;
                    padding: 10px 14px;
                    color: #fff;
                    font-size: 14px
                }

            nav.main-nav li.active > .submenu {
                display: block
            }

        /* seta animada */
        .dropdown-arrow i {
            transition: transform .28s ease
        }

        nav.main-nav li.active > a .dropdown-arrow i {
            transform: rotate(180deg)
        }

        /* mobile button (custom) */
        .mobile-toggle {
            display: none;
            background: none;
            border: 0;
            color: #fff;
            font-size: 26px;
            cursor: pointer;
        }

        /* responsivo */
         (max-width: 991px) {
            .header-banner {
                padding: 12px 20px;
                height: 420px
            }

            nav.main-nav ul {
                gap: 18px
            }

            .logo img {
                width: 150px
            }
        }

        /* mobile specific */
         (max-width: 767px) {
            .mobile-toggle {
                display: block
            }

            nav.main-nav {
                display: none;
                position: absolute;
                top: 100%;
                left: 0;
                width: 100%;
                background: rgba(0,0,0,0.85);
                padding: 12px 16px;
                flex-direction: column
            }

                nav.main-nav.open {
                    display: flex
                }

                nav.main-nav ul {
                    flex-direction: column;
                    gap: 0;
                    width: 100%
                }

                nav.main-nav li {
                    width: 100%
                }

                nav.main-nav a {
                    color: #fff;
                    padding: 12px 10px
                }

                nav.main-nav .submenu {
                    position: relative;
                    top: 0;
                    background: #0049b7;
                    border-radius: 4px;
                    margin-left: 8px;
                    margin-right: 8px
                }
        }

        /* util */
        .container {
            max-width: 1300px;
            margin: 0 auto;
            padding: 0 16px
        }
    </style>
</head>
<body>

    <!-- topbar -->
    <div class="topbar">
        <div class="container">
            <a href="https://fieb.edu.br/soualuno" target="_blank">Sou Aluno <i class="fas fa-book-reader"></i></a>
            <a href="https://fieb.edu.br/soucolaborador" target="_blank">Sou Colaborador <i class="fas fa-users"></i></a>
            <a href="https://fieb.edu.br/souresponsavel" target="_blank">Sou Responsável <i class="fas fa-user-shield"></i></a>
        </div>
    </div>

    <!-- header / banner -->
    <header class="header-banner">
        <div class="header-inner container">

            <!-- logo -->
            <div class="logo">
                <a href="/"><img src="https://stgwebfiebbrs001.blob.core.windows.net/web/logo-fiebtech-branco.svg" alt="FIEBTECH"></a>
            </div>

            <!-- mobile toggle (aparece em <=767px) -->
            <button class="mobile-toggle" aria-label="Abrir menu" id="mobileToggle"><i class="fas fa-bars"></i></button>

            <!-- nav -->
            <nav class="main-nav" id="mainNav" aria-label="Menu principal">
                <ul>
                    <li>
                        <a href="#" class="has-sub">A FIEB <span class="dropdown-arrow"><i class="fas fa-chevron-down"></i></span></a>
                        <ul class="submenu" aria-label="submenu A FIEB">
                            <li><a href="https://fieb.edu.br/historia">História</a></li>
                            <li><a href="#">Institucional</a></li>
                        </ul>
                    </li>

                    <li><a href="#">Cursos</a></li>

                    <li>
                        <a href="#" class="has-sub">Unidades <span class="dropdown-arrow"><i class="fas fa-chevron-down"></i></span></a>
                        <ul class="submenu" aria-label="submenu Unidades">
                            <li><a href="https://fieb.edu.br/unidade/aldeia-da-serra/">Aldeia da Serra</a></li>
                            <li><a href="https://fieb.edu.br/unidade/alphaville/">Alphaville</a></li>
                            <li><a href="https://fieb.edu.br/unidade/engenho-novo/">Engenho Novo</a></li>
                            <li><a href="https://fieb.edu.br/unidade/jardim-belval/">Jardim Belval</a></li>
                            <li><a href="https://fieb.edu.br/unidade/jardim-paulista/">Jardim Paulista</a></li>
                            <li><a href="https://fieb.edu.br/unidade/maria-cristina/">Maria Cristina</a></li>
                            <li><a href="https://fieb.edu.br/unidade/parque-imperial/">Parque Imperial</a></li>
                        </ul>
                    </li>

                    <li><a href="#">Notícias</a></li>
                    <li><a href="#">Contato</a></li>
                </ul>
            </nav>

        </div>
    </header>

    <main class="container" style="padding:40px 16px;">
        <!-- conteúdo exemplo -->
        <h1></h1>
        <p></p>
    </main>

    <script>
        // DOM defensivo
        (function(){
          const mobileToggle = document.getElementById('mobileToggle');
          const mainNav = document.getElementById('mainNav');

          if (mobileToggle && mainNav) {
            mobileToggle.addEventListener('click', () => {
              mainNav.classList.toggle('open');
              // mudar aria-expanded
              const expanded = mainNav.classList.contains('open');
              mobileToggle.setAttribute('aria-expanded', expanded ? 'true' : 'false');
            });
          }

          // dropdown behavior: works for desktop and mobile
          const topLevelLinks = document.querySelectorAll('nav.main-nav > ul > li > a.has-sub');
          topLevelLinks.forEach(link => {
            link.addEventListener('click', (e) => {
              const parentLi = link.parentElement;
              const submenu = parentLi.querySelector('.submenu');
              if (!submenu) return; // no submenu
              e.preventDefault();

              // close others on same level
              document.querySelectorAll('nav.main-nav > ul > li.active').forEach(li => {
                if (li !== parentLi) li.classList.remove('active');
              });

              parentLi.classList.toggle('active');
            });
          });

          // close submenus clicking outside
          document.addEventListener('click', (e) => {
            const nav = document.getElementById('mainNav');
            if (!nav) return;
            if (!nav.contains(e.target) && !mobileToggle.contains(e.target)) {
              nav.querySelectorAll('li.active').forEach(li => li.classList.remove('active'));
              nav.classList.remove('open');
              mobileToggle.setAttribute('aria-expanded','false');
            }
          });

          // keyboard: ESC closes menus
          document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') {
              document.querySelectorAll('nav.main-nav li.active').forEach(li => li.classList.remove('active'));
              if (mainNav) mainNav.classList.remove('open');
              if (mobileToggle) mobileToggle.setAttribute('aria-expanded','false');
            }
          });
        })();
    </script>

</body>
</html>
